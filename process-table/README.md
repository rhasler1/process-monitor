
### Info:
process_table provides both the domain model for
representing and querying processes and an optional
view-state implementation for presenting that model
as a process table. Consumers are not required to
use the provided view state and may define their
own presentation state when their application's
requirements differ.
